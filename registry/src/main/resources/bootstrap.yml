spring:
  application:
    name: registry
  cloud:
    config:
      uri: http://config:8888
      # fail-fast: true

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0